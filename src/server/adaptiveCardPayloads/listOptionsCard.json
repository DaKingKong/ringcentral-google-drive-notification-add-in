{
    "type": "AdaptiveCard",
    "$schema": "http://adaptivecards.io/schemas/adaptive-card.json",
    "version": "1.5",
    "body": [
        {
            "type": "TextBlock",
            "text": "Show list for:",
            "wrap": true
        },
        {
            "type": "ActionSet",
            "actions": [
                {
                    "type": "Action.Submit",
                    "title": "Active Subscriptions",
                    "data": {
                        "actionType": "activeSubList",
                        "botId": "${botId}",
                        "groupId": "${groupId}"
                    }
                },
                {
                    "type": "Action.Submit",
                    "title": "Paused Subscriptions",
                    "data": {
                        "actionType": "pausedSubList",
                        "botId": "${botId}",
                        "groupId": "${groupId}"
                    }
                }
            ]
        }
    ]
}